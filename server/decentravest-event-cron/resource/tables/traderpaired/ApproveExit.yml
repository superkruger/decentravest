DependsOn: AthenaDB
Type: AWS::Glue::Table
Properties:
  CatalogId: !Ref AWS::AccountId
  DatabaseName: "blockchain-events-${opt:stage, 'dev'}"
  TableInput:
    Name: traderpaired_approveexit
    Description: ApproveExit events
    TableType: EXTERNAL_TABLE
    Parameters: {
      "classification": "json"
    }
    StorageDescriptor:
      OutputFormat: org.apache.hadoop.hive.ql.io.HiveIgnoreKeyTextOutputFormat
      Columns:
      - Name: blockNumber
        Type: bigint
      - Name: id
        Type: string
      - Name: returnValues
        Type: struct<id:bigint,wallet:string,trader:string,investor:string,approveFrom:string,allocationInvested:bigint,allocationTotal:bigint,eventDate:int>
      InputFormat: org.apache.hadoop.mapred.TextInputFormat
      Location: "s3://${self:custom.eventbucket}/traderpaired-approveexit"
      SerdeInfo:
        SerializationLibrary: org.openx.data.jsonserde.JsonSerDe
